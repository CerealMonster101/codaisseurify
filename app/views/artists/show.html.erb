<h1><%= @artist.name %></h1>

<%= image_tag @artist.image_url %>

<h4> List of Songs by <%= @artist.name %></h4>

<% if @artist.songs.any? %>
  <ul>
    <% @artist.songs.each do |song| %>
      <li><%= song.name %></li>
    <% end %>
  </ul>
<% else %>
  <p>There are no songs in this artist.</p>
<% end %>

<%= link_to 'Delete Artist', edit_artist_path(@artist) %> |
<%= link_to 'Back', artists_path %>
